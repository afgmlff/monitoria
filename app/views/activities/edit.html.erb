<h3>Editar monitoria</h3>
<%= form_for @activity do |form| %>

    <p>
        Nome da disciplina: 
    <%= form.collection_select(:subject, Discipline.all, :id, :name) %>
    </p>
    <p>Data de inicio: <%= form.date_field :start_date %></p>
    <p>data de termino: <%= form.date_field :end_date %></p>
	<% if current_user.role == "administrator" %>
		<p>Descrição: <%= form.text_field :status %></p>
	<% else %>
    	<%= form.hidden_field :status, :value => "Aguardando confirmação" %>
    <% end %>
    <%= form.submit "Criar" %>
<% end %>